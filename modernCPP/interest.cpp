#include <iostream>

using namespace std;
int main(int argc, char *argv[]) {
	float total=atof(argv[1]);
	std::cout << total << std::endl;
	float interest = 2916;
	interest * ((1+i)^60 - 1) = total * i * (1+i)^60;
}